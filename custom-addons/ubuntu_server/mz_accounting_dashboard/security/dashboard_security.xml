<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Record Rules for Dashboard -->
    <record id="account_journal_dashboard_rule_company" model="ir.rule">
        <field name="name">Account Journal Dashboard Multi-Company</field>
        <field name="model_id" ref="account.model_account_journal"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        <field name="groups" eval="[(4, ref('account.group_account_user'))]"/>
    </record>
    
    <!-- Additional security for sensitive dashboard data -->
    <record id="account_journal_dashboard_readonly_rule" model="ir.rule">
        <field name="name">Account Journal Dashboard Read Only for Basic Users</field>
        <field name="model_id" ref="account.model_account_journal"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('account.group_account_invoice'))]"/>
        <field name="perm_read" eval="1"/>
        <field name="perm_write" eval="0"/>
        <field name="perm_create" eval="0"/>
        <field name="perm_unlink" eval="0"/>
    </record>
</odoo>